const Reading = async () => {
    return (
        <main className="relative mb-20 flex h-fit w-full flex-col mouse:mb-0 mouse:mt-20">
            <h1 className="mx-auto my-20 text-4xl font-semibold tracking-wide">Reading</h1>
        </main>
    );
};

// TODO background image (remove)
{
    /* <main
    className="mb-20 flex min-h-[200vh] w-full justify-center mouse:mb-0 mouse:mt-20"
    style={{
        backgroundImage: `url("/cook.webp")`,
        backgroundSize: "contain",
    }}
>
    <h1 className="text-4xl font-semibold tracking-wide">Reading</h1>
</main> */
}

export default Reading;
